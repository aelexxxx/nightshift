Tonight is the WEEKLY STRATEGY REVIEW for {name} — {today}.

This run is about steering, not rowing. Follow this protocol instead of the
normal nightly one (orient exactly as usual first: state, memory, inbox,
budget, issues — inbound replies still outrank everything).

1. **Goals audit.** For every goal in your Goals block: trend over the last
   7 days (read kpis.csv, compare to the week before), on/off track for the
   deadline, and the leakiest funnel stage behind the most lagging goal
   (analytics-kpis skill). Blind spots — goals with no data — become
   instrumentation tasks with top priority.
2. **Experiments audit.** Read memory/EXPERIMENTS.md. Close every experiment
   that hit its sample/duration/kill threshold with an honest verdict and a
   LESSONS.md entry. Zombie experiments (no data flowing) get killed with a
   note why.
3. **Channel ranking.** Rank active channels by results-per-effort this week
   (journal + kpis + outbox). Name the winner and the loser explicitly.
4. **Re-plan.** Pick next week's 1–2 experiments with ICE scoring against the
   most lagging goal (experiment-design skill). Write new EXPERIMENTS.md
   entries with hypothesis, sample, kill criteria BEFORE any execution.
5. **Hygiene.** Product: close/kill stale issues, curl deployed URLs to
   verify they're alive (product-shipping skill). Memory: prune STATE.md to
   current reality; archive dead threads.
6. **Strategy memo.** Update the "Current strategy" section of STATE.md in
   3–6 sentences: what we believe now, what we're betting on next week, what
   we stopped believing (with the evidence).
7. If budget and turns allow, execute the highest-priority instrumentation
   or experiment-setup task from above — reviews that ship nothing are
   theater.
8. Journal as always (journal/{today}.md). Your FINAL message is the weekly
   Morning Report: lead with goal trends and the strategy memo, then the
   experiment verdicts and next week's bets, then "Needs from you" and
   what you executed tonight.
{extra}
